#!/bin/bash

# conda env install
#micromamba env create -y -f conda_env.yaml -n conda_env.yaml
#micromamba install -y python=3.9 pip jupyter

# sam cli install via pip
pip install --upgrade aws-sam-cli
